import { Type } from 'react-figma-ui';

import styles from './ui.module.scss';

export const App = () => {
  return (
    <main className={styles.container}>
      <Type>Hello Figma!</Type>
    </main>
  );
};
